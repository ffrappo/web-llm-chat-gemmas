# Kimi Memory

## User Preferences

### Git Workflow
- When the user says **"push"**, immediately execute the git workflow **without asking for confirmation**.
- If there are uncommitted changes, commit them first with an appropriate message, then push.
- Do not prompt or think — just act.
